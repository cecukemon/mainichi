# Decision Log

A running record of *why* key choices were made — design decisions, technical
decisions, anything the user weighed in on, and anything that seemed important
enough to remember later. Newest session at the bottom; within a session,
entries are roughly in the order they were decided.

Format per entry: **Title** — the decision. *Why:* the reasoning. *Notes:*
alternatives, who decided, or follow-ups where relevant.

---

## 2026-06-28 — Session 1 (spec review → extraction prototype → data model)

**D1. Mobile-only architecture, no backend.** The app talks directly to third-party APIs and stores everything locally.
*Why:* single user, single device. A backend adds hosting, a server DB, a deploy pipeline, and monitoring — real ops cost for benefits not needed yet (server-held keys, multi-device sync, batch jobs).
*Notes:* Decided with the user, who raised the complexity concern. Mitigation baked in: every external call and the validation/segmentation step sit behind an interface, so a thin serverless proxy can be introduced later without rearchitecting (spec §10.1).

**D2. REST via `dio`/`HttpClient`, not an SDK.** Call the Anthropic and Google APIs over plain REST.
*Why:* there is no official Anthropic Dart SDK (official SDKs exist only for Python, TS, Java, Go, Ruby, C#, PHP). The features needed — vision, structured outputs, prompt caching — are just JSON fields, so a typed SDK would add little. The user asked whether adding an SDK would help; it would not.

**D3. Model selection.** Extraction → `claude-opus-4-8` (accuracy matters, volume low). Generation & grading → `claude-sonnet-4-6` (frequent, latency-sensitive).
*Why:* match capability to the job; cost is irrelevant for one user.
*Notes:* Generation quality is the riskiest assumption (spec §8 step 2) — validate it on Opus first to set a quality ceiling, then confirm Sonnet holds. Model ids rotate; verify before shipping.

**D4. Structured outputs everywhere.** Extraction and generation use `output_config.format` (JSON schema) / strict tools, not prompt-only JSON.
*Why:* guarantees parseable, schema-conformant responses. Validated live in the extraction prototype.

**D5. Furigana from the authoritative store, rendered per token.** Readings come from the vocab store (reviewed at import), never from LLM-generated readings. Layout is per-token (reading-over-base), aligned to the vocab entry each word maps to.
*Why:* LLM readings can be wrong on ambiguous kanji; the store is the source of truth. Per-token keeps Flutter layout tractable (no built-in ruby-text widget).
*Notes:* Furigana matters a lot to the user for learning → treated as an early de-risking spike (spec §8 step 2). Paired with a **contextual reading validation** check (D6-adjacent) because the model's self-reported vocab mapping is not an independent verification.

**D6. Scope-leakage validation needs segmentation; deferred.** Generated output is segmented and each content word checked (by base form) against the known-vocab set.
*Why:* preserves the "only what I've learned" guarantee that a prompt constraint alone can't ensure. Japanese has no spaces and conjugates, so this needs morphological segmentation + lemmatization.
*Notes:* Not a problem at beginner stage (worksheets arrive pre-segmented, little conjugation). Solvable in Dart (FFI to MeCab, or a lighter tokenizer), but the mature analyzers are C/Java — **this is the single most likely reason a backend would ever be needed** (spec §9, §10.1).

**D7. Prompt caching for the constraint context.** The vocab + structure library injected into every generation call is cached (`cache_control`).
*Why:* it's a large stable prefix; caching cuts cost ~10× and reduces latency for the interactive exercises. The user asked to prioritise this. Distinct from the local generated-content cache.

**D8. Surface the raw STT transcript in the speaking exercise.** Show what the recognizer heard alongside the verdict.
*Why:* a wrong/empty result has two causes — mispronunciation or STT mishearing a correct utterance. Showing the transcript keeps an STT failure from reading as "you got it wrong." Decided with the user.

**D9. Offline mode is a later feature, but design for it now.** Every online call sits behind an interface that can report "unavailable offline"; the actual mode is built after the exercises exist.
*Why:* the user wants to use the app with limited connectivity. Cheap to design for early, expensive to retrofit. Falls back to the generated-content cache for reading/listening.

**D10. API key stored outside the repo.** Key lives at `~/.config/anthropic/key` (chmod 600), read inline at run time; never pasted into chat.
*Why:* can't be swept into a future `git add`; its value never lands in the transcript. Live extraction calls are real/billed.

**D11. Extraction prototype findings → encoded rules.** After validating extraction live on real worksheets:
- **Kanji captured only if printed** — the app never adds kanji you haven't been taught, even when the model knows it.
- **Printed vs handwritten non-Japanese text** are different: printed German glosses (textbook) are legitimate meanings; handwritten German (your notes) is separated out and recorded as skipped.
- **Unique slot names** within a template; a fixed word stays in template text, not a slot.
- **na-adjective kana** normalized to the bare reading (`きれい`, not `きれい(な)`).
*Why:* these are what the real worksheets actually look like (rotation, dense handwritten German, picture-only meanings, conjugation, suffix derivation). All confirmed working after prompt fixes.

**D12. Slots carry a conjugation `form`.** A template slot has a role *and* a form (dictionary / negative / polite / …).
*Why:* the prototype showed the model improvising `{i_adjective(-く)}` for negatives. Conjugation belongs in the slot schema, not baked into the placeholder string — so the vocab entry stays in base form while the slot demands the inflection.

**D13. Data model = drift/SQLite, with these choices.**
- **Dedup** vocab on `(kana, kanji, role)`; `kanji` defaults to `''` (not null) so kana-only words actually dedup. Same kana + different role is allowed (suffix じん vs a noun).
- **`draft` → `approved` status** on words and structures gates generation, so an un-reviewed extraction draft can never leak into practice.
- **`Imports` table** keeps the raw draft JSON per worksheet for re-review/debug without re-calling the API.
- **Generated conversations** are first-class cached objects with link tables to the vocab/structures they exercise.
- **Enums stored by name** (textEnum); the extractor's snake_case strings are mapped at the import boundary via `fromExtraction` helpers, so wire and storage formats evolve independently.

**D14. SRS reconciliation — state per word/structure; conversations as the delivery vehicle.** The spec was ambiguous (§6 says SRS schedules vocab/structures; §10.3 says it schedules cached conversations). Resolved as: knowledge strength and due dates live per word/structure (so growth and solid-vs-shaky stats are per item); cached conversations are selected to cover whichever items are due.
*Why:* keeps per-item stats direct and matches §6's framing while honoring §10.3's "stable items" intent (the cache provides stable content regardless of where SRS state lives). **Confirmed by the user.**

**D15. Documentation process.** Maintain `decision-log.md` (this file) and `project-status.md`.
*Why:* the user wants a durable record of rationale and a re-orientation map across sessions.
*Notes:* The decision log is updated proactively without being asked. `project-status.md` updates are proposed to the user for review before applying.

**D16. Named speakers in generated conversations.** Speakers are drawn from `name`-role vocabulary (鈴木/すずき, 田中/田なか, katakana スミス), not anonymous A/B.
*Why:* the user wants conversations to feel natural and to get reading practice on names. Names render with furigana from the store like any other word; foreign names in katakana are supported. Decided with the user.

**D17. Generation output = per-line speaker + per-token vocab mapping; vocab-scope is the hard guarantee.** The model returns each line's speaker (name id), the full text, the structure it instantiates (or 0), and the line split into tokens each tagged with the vocab id it came from (0 = grammatical glue). Furigana is rendered from the store via those ids; scope is validated by checking every id ∈ the known sets.
*Why:* gives furigana (D5) and scope-checking (D6) a concrete handle. Validated live: scope passed on every run, furigana round-tripped, negative slot form produced correct conjugation (おもしろく/ふるく).
*Notes:* Live testing showed the model **productively recombines** known pieces into patterns not literally in the structure library (e.g. `あれは {noun} ですか`, `それは {iadj} です`) while staying within known vocabulary. Treating **vocab-scope as the hard line and structure-match as soft** is the working stance — surfaced to the user as an open question (strict template-matching vs productive recombination).

**D18. Generation model: `claude-sonnet-4-6` confirmed.** The open tier question (D3) is resolved.
*Why:* live comparison on the same seed showed Sonnet's quality comparable to Opus for constrained generation (Sonnet actually handled the negative-adjective answer more coherently in one run). Sonnet is cheaper, faster, and its lower minimum cacheable size (2048 vs Opus 4096 tokens) means prompt caching engages sooner. Prompt caching confirmed working live (cache_read=2711 across calls).
*Notes:* Coherence finding to carry forward — generated answers sometimes mismatch the question type (はい to a what-question; a noun answer to an adjective question) because the structure library lacks some answer patterns. Generation coherence scales with how complete the captured answer-pattern set is; a prompt nudge to keep Q/A type-consistent is a cheap near-term improvement.

**D19. Productive recombination allowed; vocabulary is the only hard scope line.** The generator may recombine known particles, the copula です, the question marker か, and listed patterns into natural sentences that aren't literally in the structure library — as long as every *content word* comes from the vocabulary.
*Why:* the user chose this (stance A) for richer, more interesting practice. Recombining grammar you already know (は/です/か) isn't off-syllabus; the real "only what I've learned" line is the vocabulary. Resolves the open fork from D17. The generation prompt was updated to state this explicitly and to require answers to match their question type (the D18 coherence fix).

**D20. Verb round (quick validation, user-requested): two real scope-leak bugs found and fixed.** Extended the generation seed with verbs (たべる/のむ/いく — masu-form conjugation is a bigger surface-form change than the i-adjective negative already tested) and re-ran generation live.
- **Bug found:** the model rendered conjugated verbs in invented kanji (食べます) for a vocab entry with no kanji taught, even though the `vocab_id` attribution itself was correct — a real "only what I've learned" violation that the scope validator didn't catch (it only checked kanji hiding in *glue* tokens, not kanji in content tokens).
  *Fix:* validator now checks content tokens too, keyed on `kanji.isEmpty` (not the `kanaOnly` flag — a word can have real-world kanji not yet captured/taught, e.g. たべる/食べる, which is a distinct case from an intrinsically kana-only word like ある). Prompt updated to state the rule explicitly and to mark kana-only entries in the constraint context. Confirmed fixed live across 2 follow-up runs (16 lines, zero leaks).
- **Bug found:** the model added an honorific suffix (さん) not in vocabulary, tagged it as grammatical "glue" (vocab_id 0), which is invisible to the kanji-in-glue check since さん has no kanji.
  *Fix (partial):* added an explicit prompt rule against inventing honorifics/discourse particles. Confirmed no recurrence in follow-up runs.
  *Not fully solved:* any token tagged "glue" is currently unchecked *by design* (particles/copula are assumed safe) — there is no independent verification that a kana-only "glue" token is actually known grammar rather than an invented word. This is the self-report-reliability risk from D6/D17, now concretely demonstrated. A real fix (a curated allowlist of known grammar glue, validated per level) is deferred — see open questions in `project-status.md`.
- **Also confirmed:** verb conjugation was grammatically correct across all three verb classes (godan のむ→のみ-, godan いく→いき-, ichidan たべる→たべ-); Q&A coherence extended cleanly to verb yes/no questions after adding one instruction bullet; names correctly used both as speaker and inline in sentence text; prompt caching kept working as the seed grew.
- **Design principle recorded:** on a scope violation, the app's policy is to regenerate or flag (per spec §10.3) — the renderer should not try to silently repair bad model output (e.g. substituting store kana for leaked kanji), since that risks displaying a wrong conjugated form. The validator is the authoritative gate; a failing conversation is discarded, not patched.
*Why this round:* the user asked whether extending to verbs was worth it before moving on; agreed it was cheap and the best available stress test of self-reported token attribution. Recommendation validated — verbs surfaced two real, fixable bugs the copula-only seed could not have found.

**D21. Glue-token trust gap closed with a curated, reviewable grammar-glue allowlist (not template-derived).** Any token tagged "glue" (vocab_id 0) is now checked against `knownGrammarGlue` — an explicit, hand-maintained set of the particles/copula forms the structure library actually relies on (は, を, に, か, です, では, ありません, はい, いいえ, この) plus punctuation. A glue token that doesn't factor into these pieces is flagged.
*Why this design, not others considered:*
- **Not auto-derived from template text** — a template's fixed (non-slot) text can itself be real vocabulary (これ/それ/あれ/なん are written directly into several templates, not slotted), so "any substring of a template" isn't a safe definition of "pure grammar." An explicit set sidesteps that ambiguity.
- **Curated the same way as vocabulary** — grammar glue gets the same discipline as the vocab store (a small, explicit, extend-by-hand set) rather than being implicitly trusted. Promoting it into an actual reviewable DB table (alongside Words/Structures) is a reasonable future step, deferred for now — flagged as an open question.
- **Grounded empirically, not assumed** — the set was built from live `DEBUG_RAW` token dumps across all four structure families (copula, negative copula, i-adjective, verb), not from textbook-grammar assumptions.
*A real complication found and fixed during validation:* the model's tokenization granularity for these endings is **not stable across calls** — です/か and では/ありません were sometimes split into two tokens, sometimes fused into one (both observed live, same seed, same day). An exact-string allowlist match flagged the fused form (ではありません) as a false positive on a live run. Fixed by checking whether the surface **factors** into a concatenation of known pieces (regex alternation with backtracking) rather than requiring an exact match — this tolerates the model's variable granularity without weakening the actual check (every character still has to trace to known material). Confirmed: the exact failing case now passes on 3 repeat runs; さん (and a synthetic "さんは" adjacency case) are still correctly rejected.
*Known residual risk (documented, not solved):* the factoring check only recognizes known *grammar* pieces, not vocabulary — if the model ever fuses a vocab word directly into a "glue" token (e.g. hypothetically tagging これは as one vocab_id-0 token instead of splitting これ out), it would still be flagged as unrecognized even though it's benign. Not yet observed live; noted as a possible future false-positive source rather than pre-emptively widened, since widening the check to also factor in vocabulary kana would blur the vocab/grammar distinction this fix is built on.

---

## 2026-07-02 — Session 2 (on-device DB connection)

**D22. Repository/query layer deferred to §1, not built speculatively in §0.** Considered writing thin CRUD repository interfaces (per spec §10.1) for all 9 drift tables now, to close out §0 in one pass. Rejected: on inspection, `database_test.dart`'s "dedup" test only proves SQLite's unique constraint throws on a raw double-insert — there's no tested app-level lookup (`findExisting`/`findOrCreate`) to wrap yet, and 5+ tables (`ExampleSentences`, `Imports`, `GeneratedConversations`, both link tables) have zero callers today.
*Why:* writing repository methods with no real call site is exactly the premature-abstraction risk — the shape would be guessed, not derived, and the one method that actually matters (dedup-on-import: attach an example to an existing word rather than duplicating) doesn't exist as a proven pattern yet. Building it against the real import-commit layer in §1 gets the shape right the first time.
*Notes:* Decided with the user (chose "defer" over building partial repos now for the 3 tables with dedicated invariant tests). §0 stays `[in progress]` until the repository layer lands in §1.

**D23. On-device DB connection: `path_provider` + `NativeDatabase.createInBackground`.** `lib/data/connection.dart` resolves the app-support directory via `path_provider` and opens `mainichi.sqlite` there on a background isolate (drift's standard mobile recommendation, keeps DB I/O off the UI isolate). The directory→file-path composition is factored into a pure `dbFileIn()` function so it's unit-testable without mocking the `path_provider` platform channel; the one-line `getApplicationSupportDirectory()` call itself is left as thin, untested plumbing.
*Notes:* Wired into `main.dart` at startup (`AppDatabase(connectDb())` threaded into `MyApp` via constructor) even though no screen consumes it yet — user's call, in scope for this pass. `main.dart` is otherwise still the stock `flutter create` counter demo; untouched beyond the constructor change.

---

## 2026-07-06 — Session 3 (code-vs-spec review → three fixes)

**D24. Text↔tokens reconstruction check added to scope validation.** `validateScope` now verifies that each line's token surfaces, concatenated, spell the line's `text` (ignoring whitespace — `text` separates words with ASCII or ideographic spaces, tokens don't).
*Why:* every per-token check sees only what the model put in `tokens`, and the renderer builds the display from tokens too. A word present in `text` but omitted from `tokens` was invisible to all scope checks *and* meant the learner could be shown something other than the validated line. Found in a code-vs-spec review; fully deterministic, no segmentation needed.
*Notes:* the sibling hole — a hallucinated kana surface tagged with a *valid* in-scope vocab id passes clean and would render the wrong furigana — is recorded as an open item (surface↔entry consistency check), not yet fixed.

**D25. Extraction schema emits per-slot conjugation `form` (D12 finally propagated).** The slot object in `extractionSchema` gained a required `form` field on the closed set `dictionary / negative / polite / polite_negative / past / te` (wire values of `SlotForm`), plus a prompt rule with a worked example (the i-adjective negative pattern → slot role `i_adjective`, form `negative`).
*Why:* D12 decided slots carry a form, and the drift schema, draft models, and generator all had it — but the extractor, the one component that feeds the pipeline, couldn't express it. A worksheet teaching おもしろく ありません would have arrived with every slot silently defaulting to `dictionary` at import. A test pins that every wire value round-trips through `SlotForm.fromExtraction`.
*Notes:* not yet re-validated against a real worksheet with conjugated patterns — do that on the next live extraction run.

**D26. Confirmed merge upgrades an existing entry's kanji (and fills a missing meaning) — fill-empty-only, never overwrite.** In `runCommit`, a merge now writes the draft item's kanji onto the existing word *iff* the existing kanji is empty (also clearing `kanaOnly`, since the class evidently now teaches a kanji form), and writes the meaning *iff* the existing meaning is null. A differing already-taught kanji or existing meaning is never overwritten. Upgrades are counted (`kanjiUpgradedCount`) and surfaced on the commit and done screens.
*Why:* the class routinely teaches a word's kana weeks before its kanji — the spec's own kanji-only-if-printed rule guarantees the sequence — and merge previously attached the example but *discarded* the newly-taught kanji, stranding entries kana-only forever ("Not a match" instead forked a duplicate). This was the largest domain gap found in the review: the normal life cycle of nearly every word, not an edge case.
*Notes:* guarded UPDATEs (`where kanji = ''` / `where meaning is null`), so the exact-match same-batch merge path is a natural no-op. Residual: upgrading kanji could in principle collide with another row already holding `(kana, new kanji, role)` — the unique key would throw; not pre-handled, revisit if it ever occurs. The dedup review card doesn't yet *preview* the upgrade ("will add 食べる to this entry") — UI refinement for the live-wiring pass.

**Review findings not fixed this session** are recorded in `project-status.md`: a new **Bugs** section (non-transactional `runCommit`, empty Imports row, "Not a match" silently overridden by the exact-match merge, iOS key delivery unaddressed, stale model ids) and expanded open questions (okurigana furigana for conjugated kanji words — expect a data-model change at the rendering spike; SRS grading signal undefined for reading/listening; closed-vocabulary factoring segmenter as a likely MeCab-killer; surface↔entry consistency check).

---

## 2026-07-06 — Session 4 (Capture Loop "Revised Cards" design handoff implemented)

A Claude Design handoff (`Capture Loop - Revised Cards.dc.html`) mocked up deltas against all 5 existing review-loop screens; this session brought the Flutter implementation up to date with it.

**D27. Picture-derived word gets its own review card, closing the capture-loop.md §4 open question.** Previously picture-derived words reused `VocabReviewCard` with an `if (isPictureDerived)` branch (no distinct mockup existed yet). The design handoff supplied one, so a new `QueueItemType.pictureWord` and `PictureWordReviewCard` were added — drawing-led layout, chip-picked meaning (including the handwritten gloss folded in as one more chip, not a separate affordance, per capture-loop.md §2), kanji defaulting to "No kanji" since none is ever printed alongside a drawing.
*Why:* the two item types were diverging enough (meaning-as-chips vs meaning-as-text-field, kanji provenance) that the shared card's branching was becoming the source of bugs, not a simplification. The fixture's picture-derived item previously reused the exact same kana/kanji as the plain low-confidence vocab item (both はしる/走る) as a demo shortcut, which also happened to hide this exact contradiction — the picture item now has no kanji candidates at all (`kanaOnly: true`), matching what a drawing-only worksheet entry actually looks like.

**D28. "Discard" added as a terminal state distinct from skip.** A vocab/picture-word item can now be discarded outright via a "Discard extraction" action, tracked in a new `discardedRefs` set separate from `skippedRefs`. `commit_service.dart` excludes discarded items entirely — no DB write, no "skipped" summary entry on the commit/done screens.
*Why:* capture-loop.md §3 only had one non-approval outcome (skip = revisitable deferral); the design handoff calls for a second, stronger one for genuine junk that shouldn't linger as a "skipped" item forever. Kept separate from `ReviewStatus` (which still only tracks approved/skipped/pending) so the queue-navigation plumbing (`_resolve`) could be reused unchanged.

**D29. Bulk-approve confirmed cosmetic, made explicitly undoable.** Reading `runCommit` confirmed `ReviewStatus` was never actually consulted for high-confidence items — "approve all high-confidence" was already cosmetic, just not reversible. Added `bulkApproveStaged` + `undoBulkApprove()`, and the triage screen now shows a "N high-confidence staged / not saved yet" banner with an Undo button in place of the plain approve button once tapped.
*Why:* the design handoff's triage card explicitly calls out this gap ("bulk-approve shows an applied, undoable, staged state"). Confirming the action was already non-destructive meant undo could be implemented as a pure `ReviewStatus` revert, no new state needed beyond the one boolean flag.

**D30. Kana becomes a candidate-chip field, mirroring the existing kanji pattern.** Added `kanaCandidates` to `VocabDraftItem` (defaults to `[kana]` today, same extractor limitation already noted for `kanjiCandidates` — capture-loop.md §4). Both the vocab and picture-word cards now render kana as tap-to-pick chips with a "Type (rōmaji)" fallback, instead of a free-text field.
*Why:* the design handoff's stated rationale ("no IME needed") is the same one that already justified kanji-as-chips (capture-loop.md §3) — kana just hadn't been brought in line yet. Copy-adjust of an existing interaction rather than a new one.

**D31. Dedup card gets a field-by-field match summary; `ExistingWordMatch` gained a `role`.** The dedup card now computes and displays whether reading/meaning/role individually agree between the new item and the existing Bunko entry (green if all three match, amber otherwise), rather than only showing the two entries stacked for the user to eyeball. Required adding `role` to `ExistingWordMatch` (previously only kana/kanji/meaning/examples), populated from the matched `words` row in `dedup.dart`.
*Why:* the design handoff calls this "the false-match guard the spec calls for" (capture-loop.md §3: "a same-kana false match is a real risk") — the existing stacked-comparison UI made the user do the field-by-field check mentally; this makes it explicit. Deliberately handles the non-matching case (design mockup only showed the all-match example) rather than assuming agreement.

**Also from this handoff, no new decisions needed:** all example sentences now shown on the dedup card (was `.first` only); the template card now shows the original printed sentence (`TemplateDraftItem.example`, already existed but was unused) above the generalized template; a template slot's Form dropdown is now hidden for non-conjugating roles (noun, particle, etc. — verb/i-adjective/na-adjective are the only conjugating roles) in favor of a "doesn't conjugate" note; triage now renders the worksheet photo/title/topic (`CaptureDraft.worksheetTitle`/`worksheetTopic`, already existed but were unused) and the picture-word count. None of these needed a model or architecture change — the fields already existed or the pattern already existed elsewhere.

*Verification:* `flutter analyze` clean; `flutter test` — 51/51 passing, including 2 new widget tests (bulk-approve stage/undo, discard-excludes-from-commit) and the updated end-to-end flow test. Not visually verified in a browser — this app's `sqlite3`/`drift` FFI layer cannot compile for Flutter web at all (pre-existing, unrelated to this session), so browser preview isn't a viable path for this codebase; a real device/simulator (`flutter run -d macos`/`-d ios`) would be needed for a visual check.

---

## 2026-07-06 — Session 5 (iOS API key delivery)

**D32. API key storage: `flutter_secure_storage` behind an `ApiKeyStore` interface, plus a Settings screen.** Added `lib/settings/api_key_store.dart` (`ApiKeyStore` interface, `SecureApiKeyStore` impl — iOS Keychain via `flutter_secure_storage`, `first_unlock` accessibility), `lib/settings/settings_providers.dart` (`ApiKeyNotifier`/`ApiKeyState`, same shape as `capture_providers.dart`'s DB wiring), and `lib/settings/screens/settings_screen.dart` (paste-and-save form when no key is set, masked "key saved" card with a Remove action once one is). Wired a settings icon into `HomeScreen`'s `AppBar` in `main.dart`, overriding `apiKeyStoreProvider` with the real Keychain impl at the app root.
*Why:* closes the project-status.md Bugs entry "iOS API key delivery is unaddressed" — the CLI tools read the key from `~/.config/anthropic/key`, a path that doesn't exist on a phone, and live in-app extraction/generation can't be wired up at all without some on-device story for the key first.
*Notes:* scope is storage + UI only — this does not yet wire the key into an actual live `dio` HTTP call (`worksheet_extractor.dart`/`conversation_generator.dart` still only have CLI callers); that's the separate "live extractor call from the app" planned item. No key-format validation beyond non-empty-after-trim — Anthropic key formats aren't a contract worth hard-coding a regex against. Tested via an in-memory `FakeApiKeyStore` (`test/settings/`); the real `SecureApiKeyStore` isn't unit-tested since it needs the Keychain platform channel, unavailable under `flutter test` — matches how `connection.dart`'s real `path_provider` call is left untested in favor of testing the pure `dbFileIn()` logic around it.

*Verification:* `flutter analyze` clean; `flutter test` — 59/59 passing, including 8 new tests (5 provider-logic, 3 widget) in `test/settings/`. Not run on a simulator/device — same web-incompatibility as above blocks browser preview, and no iOS/macOS simulator was available in this session; worth a manual Keychain round-trip check (`flutter run -d ios`, enter key, background/foreground the app, confirm it's still there) before this is considered fully proven on-device.

**Confirmed live on the iOS simulator** (user-verified, start of next session): key storage + Settings screen round-trip works.

---

## 2026-07-06 — Session 6 (live extractor call from the app)

**D33. Live extraction: `dio`-based `LiveExtractionService`, plus a minimal "New import from photo" entry point.** project-status.md's two capture-flow items — "live extractor call from the app" and "in-app image capture" — were separate, but there's no way to exercise a live call from the UI without *some* photo source. Scoped with the user: build the real client and wire a thin `image_picker`-based picker now; leave resize/auto-orient (crop, rotation correction) as the still-separate follow-up. Added: `lib/extraction/extraction_client.dart` (`ExtractionService` interface, `LiveExtractionService` — `dio` instead of the CLI's `dart:io` `HttpClient`, key read fresh from `ApiKeyStore` per call so a key added mid-session works without a restart, `ApiKeyMissing` thrown distinctly from `ExtractionRefused`), `lib/extraction/extraction_providers.dart` (derives from the existing `apiKeyStoreProvider` — no new override needed at the app root), `lib/capture/draft_from_extraction.dart` (pure mapper: extractor JSON → `CaptureDraft`, reusing the `fromExtraction` enum helpers already used elsewhere), and `lib/capture/screens/photo_import_screen.dart` (camera/gallery buttons → loading state → routes into the same `TriageScreen` the fixture demo uses, or an inline error for `ApiKeyMissing`/`ExtractionRefused`/other failures). `HomeScreen` now has two buttons: the original "New import" (fixture demo) and "New import from photo" (live).
*Why:* this is project-status.md's last blocker before the whole capture loop is real rather than fixture-driven; extraction and generation were already validated live via the CLI tool (`tool/extract_worksheet.dart`), so the app just needed its own transport and a UI trigger.
*Notes:* also fixed a real bug this change exposed — `CaptureQueueNotifier`'s constructor used to seed a demo Bunko entry unconditionally (`_init()`), which would have run for a live import too (polluting the user's real vocabulary store with a fake たべる entry the moment `captureQueueProvider` was first touched, regardless of which button led there). Split into two explicit methods instead: `loadDemoFixture()` (seeds + loads the fixture, called by the demo button) and `loadFromExtraction(CaptureDraft)` (loads a live draft, never seeds). `test/capture/capture_flow_test.dart`'s four tests were updated to call `loadDemoFixture()` explicitly via a `ProviderContainer`/`UncontrolledProviderScope`, since the constructor no longer auto-loads anything.
Also added iOS's required `NSCameraUsageDescription`/`NSPhotoLibraryUsageDescription` to `ios/Runner/Info.plist` — missing entirely before this session; `image_picker` would have crashed the app on first camera/library access without them, not merely denied the permission.
`draft_from_extraction.dart`'s mapper deliberately leaves `handwrittenGloss`/`newExampleSentence` unset (unlike the hand-built fixture, which sets them directly as a demo shortcut) — the extractor doesn't attribute either to a specific vocab item yet (word-boundary matching, deferred per spec §9), so a live draft has neither until that's solved.
Refactored `tool/extract_worksheet.dart`'s private `_mediaTypeFor` into a shared `mediaTypeForPath` in `worksheet_extractor.dart`, reused by both the CLI and the new in-app client.
*Verification:* `flutter analyze` clean; `flutter test` — 71/71 passing, including 3 new suites (`extraction_client_test.dart` against a fake `dio` `HttpClientAdapter` — no real network call, this never hits the paid Anthropic API from automated tests; `draft_from_extraction_test.dart`; `capture_providers_test.dart` for the split load methods) plus a `photo_import_screen_test.dart` widget suite faking `ImagePickerPlatform` and `ExtractionService`. **Not exercised against the real Anthropic API or a real device camera** — that needs the user's own key and simulator, same as the Settings screen check in session 5.

---

## 2026-07-06 — Session 7 (two cheap correctness bugs: transactional commit + non-empty Imports row)

**D34. `runCommit` is now atomic, and it writes real import provenance.** Two of the project-status.md **Bugs** entries, fixed together since both touch the commit path.
- *Transaction:* `runCommit`'s body moved into a private `_runCommit`, wrapped in `db.transaction(...)`. The Imports insert plus all word/template/example writes now commit or roll back as one unit, so a crash (or a mid-commit throw) can no longer leave a half-imported worksheet with a dangling Imports row. Proven by a new test that forces a mid-commit collision (the unguarded kanji-upgrade unique-key clash noted in D26) and asserts both the earlier word insert *and* the Imports row are rolled back.
- *Provenance:* the Imports row was being inserted with `rawDraftJson: null` and no `sourceImage`/`model`, defeating the table's whole purpose (D13: re-review/debug an import without re-calling the API). `CaptureDraft` gained three nullable provenance fields (`sourceImage`, `model`, `rawDraftJson`); `draftFromExtraction` now captures the raw draft verbatim (`jsonEncode` of the extraction map) and threads the photo path + `extractionModel` through from `PhotoImportScreen`; `runCommit` writes all three onto the Imports row.
*Why:* both were flagged in the 2026-07-06 code-vs-spec review and explicitly deferred until the live extractor call existed (session 6) — the provenance fix in particular was meaningless while the pipeline was fixture-only. Now that a real photo/model/JSON flows through, the row can carry it.
*Notes:* provenance is null on the hand-built demo fixture (`buildSampleDraft`) — it has no real photo or model response behind it, so null is the honest value there, not a regression. The transaction wrapper doesn't change any success-path behaviour; all pre-existing commit tests pass unchanged. The residual D26 kanji-upgrade collision is still not *prevented* (only now exploited as a convenient way to force a rollback in the test) — it remains its own open item.
*Verification:* `flutter analyze` clean; `flutter test` — 75/75 passing, including 4 new tests (Imports-row provenance write, atomic rollback on mid-commit failure, and two `draftFromExtraction` provenance-capture cases).

---

## 2026-07-06 — Session 8 (furigana rendering spike + okurigana + surface↔entry check)

**D35. Okurigana: derived at render time from the existing (kana, kanji) pair — the predicted data-model change turned out to be unnecessary.** The spike's expected outcome was a stem/okurigana split forcing a schema migration. Instead: the store already holds both base forms, and their **longest common kana suffix** *is* the okurigana — 行く/いく → stem 行, reading い, okurigana く; 食べる/たべる → 食/た/べる; 田なか/たなか → 田/た/なか. For regular verbs/adjectives the kanji stem is invariant across conjugation, so a conjugated surface (行きます) decomposes into stem + pure-kana tail by prefix match alone — no conjugation engine, no new columns. Implemented as a pure, Flutter-free module `lib/japanese/okurigana.dart` (`splitOkurigana`, `furiganaSegments` → list of base/ruby segments, or null when the surface can't be reconciled with the entry).
*Why this design:* three consumers share it — the furigana widget, the scope validator, and (later) the closed-vocabulary factoring segmenter, whose stem inventory is exactly this function mapped over the store. The alternatives (stored stem columns; a full conjugation table) were rejected as a migration + drift risk for something derivable, and over-machinery respectively. **Chosen with the user** (all three spike questions).
*Known limitation, deferred with a trigger:* reading-*changing* irregulars (来る/くる → 来ます/きます) would get the wrong stem reading — but only once 来 is actually taught in kanji; the kanji-only-if-printed rule protects until then. Add an override when a real worksheet forces it.

**D36. Surface↔entry consistency check folded into `validateScope` (the D24 sibling hole, now closed).** A non-glue token's surface must be a recognizable form of the entry it claims: its kana or kanji exactly, or — for conjugating roles (`verb`/`i_adjective`/`na_adjective`, new `conjugatingRoles` const) — a stem-preserving conjugation (kanji-stem + kana tail, or pure-kana sharing the kana stem). Null from `furiganaSegments` = violation. This closes the laundering gap where a hallucinated surface tagged with a *valid* in-scope vocab id passed every id-based check and would have rendered the wrong furigana.
*Notes:* the check runs after (and is skipped by) the existing untaught-kanji check so one bad token doesn't double-report. Kana-written forms of kanji-taught words are accepted as valid (writing in kana is never wrong). Surrounding punctuation on a token is split off, not flagged. `renderConversation` (CLI) was also fixed with the same segments — it previously substituted the base form into conjugated lines (行く for 行きます) whenever the entry had kanji; now renders `行[い]きます。`. A regression test pins each of these.

**D37. Furigana widget: custom per-token implementation, not the `ruby_text` package.** The spec (§10.5) said evaluate a package first; evaluated — `ruby_text` was last published 3 years ago (780 total downloads) and pulls in flutter_hooks/equatable/tuple. Built `lib/reading/furigana_text.dart` instead (~80 lines, dependency-free): a `Wrap` of per-token `Row`s of reading-over-base `Column` stacks. A multi-segment token (行+きます) is one unbreakable Row so a word can never wrap apart mid-token; every stack reserves the same ruby band height so bases align across tokens with and without readings; the furigana toggle (spec §4) collapses the band. Plus `lib/reading/screens/furigana_preview_screen.dart` — a spike preview route on the home screen (hardcoded lines through the real split+widget pipeline, with the toggle) for the visual check on a simulator; replaced by the real reading screen later.
*Verification:* `flutter analyze` clean; `flutter test` — 98/98 passing (16 okurigana-module tests, 3 new generator tests incl. the laundering-gap repro and the renderer fix, 5 widget/preview tests). **Visual check on a simulator still to do** (the "looks right" half of the spike) — the layout logic is test-proven but typography needs eyeballs; use the home screen's "Furigana preview (spike)" button.

---

## 2026-07-06 — Session 9 (closed-vocabulary factoring segmenter)

**D38. The factoring segmenter is built and wired into `validateScope` — scope validation no longer depends solely on the model's self-report.** `lib/japanese/segmenter.dart`: `factorLine` factors a line's `text` into taught material — vocabulary base forms (kana + kanji), taught conjugations of them, the grammar-glue allowlist, punctuation/whitespace — by greedy longest-match with backtracking (dead-position memoization), the same trick `_glueFactoring` used for glue alone, now over the whole closed lexicon. Runs per line in `validateScope` with no reference to `tokens` at all; failure reports the furthest unmatched tail.
Key design choices:
- **Taught endings are derived from the structure library's slot forms**, not assumed: `taughtForms` = the `form` values on the seed's slots (+ `dictionary` always). A ます-form is in scope only once a taught pattern demands `polite`. Self-maintaining: new grammar imports new slot forms, endings unlock automatically. (Session-8's conjugation test gained a polite-slot structure to reflect this semantics.)
- **Conjugation without a verb-class column:** stems are the written base form minus its final kana (same last-char insight as the okurigana module); since godan/ichidan isn't stored, both connector candidates are generated (ichidan '' when the base ends る, godan i-row otherwise/also). Deliberately slightly over-permissive (たべります would pass) — but everything accepted is still composed purely of taught stems + taught endings, which is the guarantee the check makes. Suffix map today: polite→ます, polite_negative→ません, past→ました, i-adjective negative→く (ありません continues as glue). te-form and verbs' plain negative are deliberately unmapped (irregular-prone, not yet taught) — an unmapped form flags loudly on first contact, the knownGrammarGlue extend-on-touch discipline.
- **Module is dependency-free** (`LexiconEntry` rather than SeedWord/DB rows) so capture/import can reuse it later.
*Why:* this was the open-question endgame for D6 — the vocabulary is small and *closed*, so greedy factoring replaces general morphological analysis, is fully independent of the model's tokens, and most likely kills the only remaining argument for a backend (spec §9/§10.1).
*Validated live, 2 runs (12 lines) on the real seed:* **zero false positives** — including fused ではありません, おもしろいです, and a verb-heavy focus run (たべます/のみません/いきます across ichidan + godan rows, cache_read hit confirmed). And a **genuine catch on the very first run**: the model emitted その (untaught — only この is in scope), flagged independently by both the D21 glue check (token-level) and the new factoring check (text-level). A unit test also pins the case only factoring can catch: 行きましょう — tokens reconstruct, vocab id valid, per-token stem check passes (unbounded kana tail), only ending-gated factoring rejects.
*Also observed live, noted not fixed:* the model sometimes omits 、 from `tokens` while `text` has it (the D21 granularity nondeterminism), tripping the D24 reconstruction check — a rendering-fidelity discrepancy (the renderer builds from tokens, so the learner would see the line without its commas), not a scope leak. Left strict for now; resolve at the reading-screen pass (render punctuation from `text`, or normalize punctuation in the reconstruction compare).
*Verification:* `flutter analyze` clean; `flutter test` — 110/110 passing (11 new segmenter tests incl. backtracking, form-gating, and unmapped-form flagging; 1 new integration test; 4 existing tests updated where factoring now adds a second, corroborating violation).

---

## 2026-07-06 — Session 10 (reading exercise screen — design spec)

No code this session — the reading exercise (spec §5, §8 phase 2) is the next thing to build, and its UX wasn't decided anywhere. Discussed with the user and wrote it up as `features/reading-exercise.md`, mirroring `capture-loop.md`'s structure, ready to hand to Claude Design.

**D39. Reading exercise is a continuous feed, hands-off on topic.** One tap generates a conversation; **Next** generates and shows another (repeat indefinitely); **Exit** leaves at any point. No fixed session length (e.g. "3 conversations"), no topic/focus picker in this pass, even though the generator already supports a `focus` parameter.
*Why:* the user chose this over a fixed batch or a topic-tag picker — a session length would be an arbitrary guess with no pedagogy behind it yet, and hands-off keeps the entry to one tap. Topic steering stays available as a future addition, not exposed in v1 UI.

**D40. No self-rating or comprehension check in the reading exercise (v1).** Reading ends with a plain **Next**/**Exit**, nothing else — the user chose this directly over a per-line or per-word "got it / not yet" rating.
*Why:* per-line/per-word self-grading is a real candidate for feeding the SRS scheduler eventually, but *how* one multi-line conversation should update per-word ease is the still-open "SRS grading signal" question in `project-status.md` — building a rating UI now would be guessing at a design that question hasn't settled, and would likely need reworking once it is. Reading stays a pure comprehension exercise per spec §5, matching how it's explicitly *not* given a check step the way speaking is.

**D41. Word tap opens a full popover (reading + meaning + role, plus dictionary base form for conjugated surfaces); layout is script-style, not chat bubbles.** Tapping a token that maps to a vocab entry shows its reading, meaning, and role, and — if the surface is a conjugated form (食べます) — the dictionary base form it conjugates from (食べる). Grammatical glue (は/です/か/particles) and punctuation aren't tappable. Each line renders with its speaker's name as a label, left-aligned, script/transcript style — not chat bubbles.
*Why:* the user chose the full popover over a bare highlight or no interaction at all — it's the single most useful "am I understanding this" affordance available without a formal check step (D40), and combined with the furigana toggle (spec §4) becomes the screen's implicit self-test loop. Script layout was chosen over chat bubbles because this is a reading passage on a page (spec §5's own wording), not a messaging thread, and matches how the CLI's `renderConversation` already presents it (`Name: line`).

**D42. Punctuation renders from the line's `text`, not reconstructed from `tokens`.** Resolves the open item logged at the end of session 9: the model sometimes omits 、 from `tokens` while `text` has it (a tokenization-granularity quirk, not a scope leak), which would silently drop the comma from the display if the renderer built purely from tokens. Decided here: the reading screen takes punctuation from `text`, tokens only supply the per-word furigana mapping.
*Notes:* also decided as part of this pass — generation failures (refusal, or repeated scope-validation failure) surface as an explicit error state with a retry action, never an infinite silent retry loop, consistent with D20's existing principle that a failing conversation is discarded, not patched.

Full flow, all open questions (live `GenerationSeed` sourcing, popover-vs-ruby-band layout, generate-ahead latency, reading-correctness caveats inherited from D35/§9), and current status are in `features/reading-exercise.md` — not duplicated here.

**D43. Claude Design handoff ("Reading Exercise Mockups") received and matches D39–D42; saved into `features/` as six standalone screens** (`reading-home-entry.html`, `reading-loading.html`, `reading-error.html`, `reading-screen.html`, `reading-screen-furigana-off.html`, `reading-word-lookup.html`), same convention as the capture-loop mockup files. One direction only ("Reader"), no multiple-choice fork to resolve — unlike the earlier "Capture Loop - Revised Cards" handoff.
*What the mockup confirmed as-decided:* continuous feed (plain **Next**, no session counter), no self-rating step, hands-off generation (no topic picker on entry), errors surfaced with explicit retry (D39/D40/D42).
*What the mockup settled beyond D39–D42 (visual detail, not previously pinned down):* the speaker name sits in a fixed left margin column rather than an inline "Name: line" prefix (book-marginalia feel); the word-lookup UI is a full-width bottom sheet, not an inline popover anchored to the tapped word — which **resolves** the open question about the popover colliding with the furigana ruby band, since a bottom sheet never has to render next to the tapped token; the lookup sheet shows a role chip, and folds the slot's grammatical form into the meaning line (e.g. "to eat · negative, polite") rather than as a separate field.
*New open item surfaced by implementing-in-the-mind this mockup:* the lookup sheet's form annotation ("negative, polite") isn't data the app currently has in hand — `GenLine` only carries a line-level `structureId`, not a per-token link to which slot/form produced that token. Needs a decision (derive it by comparing the tapped surface to the vocab entry's dictionary form, vs. having generation report it per token) before the sheet can actually be built — recorded in `features/reading-exercise.md` §4, not resolved here.

---

## 2026-07-07 — Session 11 (reading exercise: form annotation derived at render time)

**D44. The lookup sheet's form annotation is derived at render time from the store, not reported per token by generation.** Resolves the open question D43 surfaced. New `detectTaughtForm` in `lib/japanese/segmenter.dart`: given a tapped surface and its vocab entry, it matches the surface against the same taught-conjugation inventory the factoring segmenter already generates (`_conjugatedForms`, generalized to yield form + surface pairs — single source of truth) and returns the slot-form wire value, or null for the base form / anything untaught. A separate presentation map (`lib/reading/form_label.dart`) turns wire values into the mockup's wording ('polite_negative' → "negative, polite"; 'past' → "past, polite", since the taught past is the polite ました).
*Why derived, not model-reported:* (1) spec §10.3's authority rule — sheet content comes from the store, never the model; a per-token form field in the generation schema would be self-report of exactly the kind D36/D38 were built to check independently, and the validator for it would *be* this derivation; (2) the machinery already existed — the endings inventory is the segmenter's, kept taught-forms-gated, so a form is only ever annotated once a structure teaches it; (3) graceful degradation is free — null (base form, untaught ending, unknown label) just means the sheet shows no form line, never wrong data. `GenLine`, the generation schema, and the prompt are untouched.
*Verification:* `flutter test` — 117/117 passing (5 new detection tests: verb classes/scripts, i-adjective negative, base-form null, untaught/wrong-entry null, punctuation peeling; 2 new label tests).

**D45. The repository layer starts as a single read-side slice: `SeedSource.loadGenerationSeed()`.** New `lib/data/seed_repository.dart`: a `SeedSource` interface (what the reading screen will depend on — fixture-implementable for tests/previews) with a `DriftSeedSource` implementation mapping approved Words and Structures+Slots rows to the generator's `SeedWord`/`SeedStructure`. Only `status = approved` items are included, upholding the existing invariant that un-reviewed drafts never reach practice; slots come back in ordinal order. Supporting change: `WordRole.wire` / `SlotForm.wire` getters in `lib/data/enums.dart` — the reverse of `fromExtraction` — since the generation engine and segmenter speak the snake_case wire format while drift stores enum names (a round-trip test pins the inverse property). One Dart wrinkle worth recording: inside `WordRole`, the enum *value* `name` shadows `Enum.name`, so the getter needs `this.name`.
*Why a slice, not the layer:* D22 said build the repository against real calls, not speculatively — this is the reading screen's real call, and the only blocking one. The write side already exists as `CommitService`; further read queries (SRS due-item selection, conversation cache lookups) get added when their callers exist.
*Verification:* `flutter analyze` clean; `flutter test` — 122/122 passing (4 new repository tests: wire-format mapping, draft exclusion, slot ordering/forms, empty store; 1 wire round-trip test).

**D46. In-app generation transport: `GenerationService`/`LiveGenerationService`, mirroring the extraction client; `ApiKeyMissing` promoted to the settings module.** New `lib/generation/generation_client.dart`: a `dio`-based service behind an interface (D9), key read fresh from the `ApiKeyStore` per call, reusing the engine's `buildGenerationRequest`/`parseGenerationResponse` — the generation HTTP call no longer exists only in the CLI tool. Transport only, deliberately: `validateScope` stays with the caller, because the screen owns how a violation surfaces (explicit error state with retry, D42), and a validation-in-the-client would hide that policy in transport code. `ApiKeyMissing` moved from `extraction_client.dart` to `lib/settings/api_key_store.dart` (re-exported from its old home so existing imports stand) — it was never extraction-specific, and now two services throw it.
*Verification:* `flutter analyze` clean; `flutter test` — 125/125 passing (3 new client tests: key-missing short-circuits before any network call, request shape incl. cached seed context + focus + line count with response parsing, refusal surfaced as `GenerationRefused`).

**D47. Reading exercise screen implemented (features/reading-exercise.md, mockups D43); the furigana-preview spike is deleted; the D24 reconstruction check is now punctuation-insensitive.** New in `lib/reading/`: `line_display.dart` (pure mapper: tokens located inside the line's `text`, whatever they don't cover renders as plain segments — punctuation authority is `text` per D42, tokens only supply furigana + tap targets, with fallbacks that render-plain rather than drop or mis-ruby anything), `reading_providers.dart` (`seedSourceProvider`, `generationServiceProvider`, and an autoDispose `readingSessionProvider` — loading/error/ready, generate on entry and on **Next**, `validateScope` re-checked in the notifier so out-of-scope output is discarded and surfaces as the error state, never shown; an empty Bunko short-circuits with "import a worksheet first" before any API call), `word_lookup_sheet.dart` (bottom sheet: furigana headline, role chip, derived conjugated reading, meaning with the D44 form annotation, dictionary-form card only when conjugated), and `screens/reading_exercise_screen.dart` (script layout with the margin-column speaker names, furigana toggle in the app bar, footer hint that flips with the toggle, Next/Exit). Home screen's "Reading practice" replaces the spike button; `furigana_preview_screen.dart` deleted outright as planned (D37's temporary preview), its toggle coverage superseded by the new screen tests.
*Reconstruction check change:* the session-9 open item (model omits 、 from `tokens`, tripping the strict D24 compare) is closed the way the D38 log deferred it: now that the renderer takes punctuation from `text` and the factoring check already covers every character of `text` including punctuation, the text↔tokens compare ignores punctuation as well as whitespace — a missing word still flags, a missing comma no longer false-rejects a valid conversation.
*Verification:* `flutter analyze` clean; `flutter test` — 140/140 passing (4 line-display tests incl. the comma-omission case and render-plain fallbacks; 10 screen widget tests incl. a fixture-passes-the-real-validator guard, lookup sheet content, glue not tappable, toggle, Next, refusal→retry, scope-leak discard, empty-Bunko; 1 new validator test pinning punctuation tolerance). Not yet done: the live end-to-end simulator run (step 5) — first in-app generation call, the pending furigana visual check, and real latency numbers.

**Live finding (user, 2026-07-07): reading screen works end to end on the simulator, but generation failed 3 of 5 tries into the "Try again" error state.** Suspected cause: the very small live vocabulary (only one–two worksheets imported) gives the model little room to compose in-scope conversations, so scope validation rejects more attempts. Not investigated yet — worth revisiting once the Bunko is bigger; if the rate stays high with a real vocabulary, the next steps are inspecting *which* check rejects (the error state doesn't currently say) and possibly logging validation failures with their violations for diagnosis.

**D48. Crop-box bug fixed: the worksheet photo boxes now show the live import's real photo.** `WorksheetCropPlaceholder` (decorative gray box, never wired to image bytes) is replaced by `WorksheetPhotoBox` (`lib/capture/widgets/worksheet_photo_box.dart`): a ConsumerWidget that reads the current draft's `sourceImage` path from `captureQueueProvider` and renders the photo (zoom → `InteractiveViewer` dialog), falling back to the old labeled placeholder for the demo fixture, a stale path, or a decode failure. Since the extractor still doesn't return per-item crop regions (the separate planned item), every box — triage's full sheet and the review cards' "crops" — shows the whole photo; honest and useful ("compare with the worksheet") until real crops exist. Provider-read rather than threading a path parameter through triage + two cards: the current import's photo is app-level state with one source of truth.
*Verification:* `flutter analyze` clean; `flutter test` — 143/143 passing (3 new widget tests: placeholder without a photo, photo renders + zooms from a real temp PNG, stale-path fallback).

**D49. Generated-content cache implemented (spec §10.3, `features/generated-cache.md`): invisible write-through plus one visible affordance.** Every conversation that passes `validateScope` is persisted the moment it reaches the reading screen's ready state — payload verbatim (`GeneratedConversation.toJson`/`fromJson`, one format for wire and cache; the response parser now shares it), link rows derived from the *validated* tokens/lines (`tokenVocabIds`/`lineStructureIds`), never the model's `used_*` self-report, `lastPracticedAt` stamped on display (the only honest practice signal in a completion-less feed, D39). A persistence failure never interrupts reading. The single visible piece: when generation fails and the cache is non-empty, the error state offers **"Reread an earlier one"** — least-recently-practiced cached conversation, rendered against the *current* store (a since-deleted word renders plain via the existing `line_display` fallback). Motivated directly by the live 3-of-5 failure rate; same path §6's offline mode will use. Explicitly not built: browse/library UI, cache caps/eviction, cache-served **Next** (freshness is the exercise's point — cached content re-enters the feed at phase 5's SRS selection, deliberately). New `lib/data/conversation_cache.dart` (`ConversationStore`/`DriftConversationStore`, injectable clock since drift stores DateTime at second resolution); wired via `conversationStoreProvider`. One naming wrinkle: drift's row class for the table is also `GeneratedConversation` — the cache module hides the row name and means the engine's type.
*Verification:* `flutter analyze` clean; `flutter test` — 151/151 passing (5 store tests: persistence + link rows, payload round-trip, LRU + markPracticed rotation, empty cache, word-delete cascades the link not the conversation; 3 screen tests: write-through with validated-token links, reread fallback serves + rotates, no offer on an empty cache).

---

## 2026-07-07 — Session 12 (listening exercise: spec drafted)

**D50. Listening exercise designed as an audio layer on the reading screen, not a separate screen — spec drafted (`features/listening-exercise.md`), no code yet.** User's framing, confirmed against spec §8's "small addition once reading works": the conversations, layout, lookup sheet, and cache are all shared; the pedagogical difference (hear-first vs. read-along) is covered by a blur-the-text listening mode (speaker margin stays visible — the two voices carry speaker identity anyway) rather than a duplicate screen. Core decisions in the spec:
- *Manual playback only* (play/pause + per-line replay in the speaker margin as the rewind control; no autoplay) — consistent with the completion-less feed (D39).
- *Per-line synthesis, two Google voices mapped to the two speakers* — comprehension aid plus trivial line replay; whole-conversation play chains the files.
- *TTS input is the store-assembled kana line* (token readings + punctuation from `text`, D42), never the kanji — Google applying its own readings would mask kana errors in the store, defeating spec §5's stated side benefit. Same authority rule as rendering (§10.3).
- *Synthesize once at 1.0×; 0.5/0.75/1.0 speed is client-side playback rate* (`just_audio` `setSpeed`) — one synthesis and one cached file per line ever, vs. per-speed synthesis with Google's `speakingRate`.
- *Lazy synthesis on first play* (not eager at generation) — a second external API must not enter the reading success path; TTS failure shows an inline player error, reading unaffected (same isolation stance as D49's write-through). Trade-off: reread/offline audio exists only for conversations played once while online — fine while §6 is deferred.
- *Content-addressed audio files* — per-line filename = hash of (kana string + voice id), `audioPath` = per-conversation directory. A kana reading corrected after synthesis just misses the cache and re-synthesizes; stale audio self-invalidates with no bookkeeping, converging audio to the live store the way rendering already does (D49).
- *`TtsService` behind an interface, Google Cloud key as a second `ApiKeyStore` slot* — the D9/D32/D46 pattern, giving §6's offline toggle its seam.
Open items recorded in the spec §4: voice selection (calibrate live), inter-line gap tuning, kana-only prosody risk (SSML `<sub>` as the fallback), per-line reveal, and whether "played while blurred" becomes the phase-5 listening practice signal.

**D51. Listening exercise implemented per D50 (`lib/listening/`), pending the live simulator run.** The pieces: `TtsService`/`LiveTtsService` (`tts_service.dart`, Google Cloud TTS REST via `dio`, key fresh per call, voice-id constants `speakerVoiceA/B` in one place because they participate in the cache key); kana line assembly (`line_audio.dart`: `kanaLine` reuses the reading screen's `displayTokens`/`readingOf` so audio and furigana speak from the same authority, `lineAudioSpecs` maps speakers to voices first-come); the content-addressed file store (`audio_store.dart`: `ensureAudio` synthesizes only missing files, sha1(voice|kana) names, `audioPath` set on first synthesis via the new `ConversationStore.setAudioPath`); `ListeningController` (`playback_controller.dart`, ChangeNotifier with a monotonic session token so stop/replay interrupts cleanly) over a `LineAudioPlayer` interface whose real implementation is `just_audio` (`just_audio_player.dart` — `setSpeed` = pitch-corrected AVPlayer rate). UI on the reading screen (D50: no separate screen): audio bar (play/stop, 0.5/0.75/1× `SegmentedButton`, listening-mode toggle), per-line replay in the speaker margin, current-line highlight, blur via `ImageFiltered` — tapping the blurred text reveals it (and deliberately does *not* open the lookup sheet). Settings gained the second key slot: `SecureApiKeyStore.anthropic()/google()` named constructors, `googleApiKeyStoreProvider`, the screen refactored into two structurally identical `_KeySection`s; `ApiKeyMissing` carries the service name.
*Supporting changes:* `ReadingSessionState.ready` now carries the cache row id (persist moved *before* the ready state — same write, the id is what the audio layer keys on; a persist failure shows the conversation without audio). `AudioStore` interface split from `ConversationAudioStore` after the first test run: real file IO deadlocks under `testWidgets`' fake clock, so widget tests use an in-memory fake and the filesystem behavior is covered by plain `test()`s. One real bug caught by the tests: the `ListenableBuilder` initially returned a body built *outside* its builder — identical widget instance, so controller notifications never repainted; rebuilt inside the builder.
*Verification:* `flutter analyze` clean; `flutter test` — 176/176 passing (25 new: 3 kana-line/voice-mapping, 5 audio-store filesystem incl. self-invalidation on corrected kana and partial-failure retry, 9 controller sequencing/error tests with a scripted player, 2 TTS transport, 1 drift `setAudioPath`, 4 screen widget tests for audio bar/play/speed/blur/error, 1 Google-slot settings test). Not yet done: the live end-to-end run (real Google key, voice calibration, chained-line gap check on device).

---

## 2026-07-18 — Session 13 (Bunko backfill from scope failures)

**D52. Scope-failure backfill: the reading screen's error state offers unmatched words for review and commits them with honest reading-backfill provenance — no schema change.** Motivated by the live failure log: rejections repeatedly named words the class *did* teach but no worksheet capture ever recorded (その — deliberately absent from `knownGrammarGlue` while この is present, "flag loudly on first contact" working as designed against stale data). This is the capture-flow direction the grammar-glue open question already predicted, generalized: validation failures become a capture surface. The pieces: `ScopeReport` now carries structured `candidates` (word-shaped unmatched surfaces — from unknown-glue tokens directly, and from factoring failures by cross-referencing the un-factorable remainder against the model's own token boundaries, falling back to punctuation/glue-boundary trimming); the error state renders them as chips ("Missing from your Bunko?") that open the capture flow's own `VocabReviewCard` in a bottom sheet, reframed ("Did your class teach this?", worksheet-comparison box hidden, approve gated on a non-empty meaning — the lookup sheet displays meaning, and the user is confirming a word they know). Approve commits through the normal `runCommit` path as a one-item `CaptureDraft` (`ScopeBackfillService`, `lib/reading/scope_backfill.dart`) — transaction, exact-duplicate merge, and approved status for free — then **re-validates the same rejected conversation against a freshly loaded seed: if the added word was the only problem, it's shown immediately** (the self-healing loop; otherwise the error returns with recomputed candidates, the added word's chip gone). Provenance: `sourceImage` null, `model` = the generation model, `rawDraftJson` = `{"source": "reading-backfill", "surface": …}` — the Imports table has no source-type column and gets none (a discriminator column + drift migration buys nothing v1 reads; the raw-material slot is the honest home since the generation model did propose the surface). Guardrails against convenience bias (every approval makes an error go away): one candidate at a time, no bulk approve, discard framed as the default answer for anything the class didn't teach.
*v1 scope, deliberately:* multi-character kana surfaces only. Single characters are almost certainly particles (deferred to the glue-table promotion — now more pressing, since they're silently filtered from chips); kanji-bearing surfaces have an unknown reading the card can't safely capture (its kana entry is free-text rōmaji, no IME assumed). Known limitation: a kana match with a *different* role inserts a near-duplicate — the card has no merge UI.

**D53. Glue-check relaxation, kana-only: a glue-tagged token whose surface exactly matches a taught word's kana passes scope validation (logged, not flagged).** Prerequisite for D52's self-healing loop, and a real design change worth its own entry: the glue check never consulted the lexicon, so after backfilling その the *same* conversation would re-reject forever — the model's その token is tagged vocab_id 0, and `knownGrammarGlue` is a hardcoded const. Rationale: when the surface is a taught word's kana, the material *is* taught — the model merely mislabeled a real word as glue, a metadata error, not a scope leak; every character is still independently verified by the factoring check, which knows nothing of token labels. Deliberately kana-only: a kanji-surfaced glue token would render without furigana (lookup keys on vocab id), so it stays a violation even when it matches a taught kanji — keeping the D20/D21 spirit (nothing unrecognized hides behind "it's just glue"). Each acceptance is logged under `reading.scope` so mislabeling frequency stays observable; if it climbs, the fix belongs in the generation prompt, not the validator.
*Verification:* `flutter analyze` clean; `flutter test` — 196/196 passing (20 new: 7 generator tests for candidate extraction/dedup/single-char and kanji exclusions/relaxation both ways, 3 `ScopeBackfillService` db tests incl. provenance row and double-commit merge, 6 reading-screen tests incl. the full self-heal round trip and the still-fails path, 4 standalone `VocabReviewCard` tests — the card's first, pinning the new `showWorksheetComparison`/`requireMeaning` params and the capture-flow defaults).

---

## 2026-07-19 — Session 14 (extraction truncation fix)

**D54. Worksheet extraction `max_tokens` raised to 16000, and a truncated response now surfaces a real error instead of a `FormatException`.** Observed live: importing a dense worksheet failed with `FormatException: Unexpected end of input (at character 17506)` — the model's structured JSON draft exceeded the 8000-token output cap and stopped mid-object (right after `"kana":"ことし","kanji":`), and `parseExtractionResponse` only guarded `stop_reason == 'refusal'`, so the partial JSON fell straight into `jsonDecode`. Two-part fix (`lib/extraction/worksheet_extractor.dart`): (1) `buildExtractionRequest`'s `maxTokens` default 8000 → 16000 — enough for a full worksheet while staying in the safe non-streaming range (the live `dio` client makes one blocking POST; a much larger cap would risk HTTP timeouts without switching to streaming, which extraction doesn't do); (2) a new `ExtractionTruncated` exception thrown on a `max_tokens` stop *before* decoding, so the partial body never reaches `jsonDecode`. `PhotoImportScreen` catches it with an actionable message ("too long to read in one pass — try part of it, or a less dense page") instead of dumping the raw exception. Deliberately not done: per-worksheet chunking — the right answer if a single page ever exceeds 16000 tokens, but not worth building before it's observed; the friendly truncation message is the interim floor.
*Verification:* `flutter analyze` clean; `flutter test` — 197/197 passing (1 new parser test: a `max_tokens` stop with a partial-JSON text block throws `ExtractionTruncated`, not `FormatException`).

**D55. Listening live run completed — D51's pending verification closed, spec §4 calibrate-live items resolved with no changes.** The end-to-end run with a real Google key (user, 2026-07-19) confirmed the whole live path: synthesis via `LiveTtsService`, content-addressed caching, and playback. The two subjective §4 items both passed as shipped: the voice pair `ja-JP-Neural2-B`/`ja-JP-Neural2-C` sounds right for beginner-paced Japanese (kept — no WaveNet comparison needed), and the natural gap between chained per-line files reads as a comfortable dialogue pause (no explicit gap insertion or SSML `<break>` machinery added, per the spec's measure-before-building stance). Phase 3 (listening exercise) is done.

---

## 2026-07-19 — Session 15 (grammar-glue table promotion)

**D56. Grammar-glue table promotion: the allowlist is now a reviewable `GrammarGlue` DB table, and single-character (particle) candidates are backfillable through the reading screen's error state — the D52 loop closed for glue.** The recurring hand-edit D52 predicted ("an untaught particle still means a hand edit to the constant" — most recently も/の/と/へ/や on 2026-07-19) is gone: `validateScope` reads its glue from `GenerationSeed.glue`, which `DriftSeedSource` loads from the new table, and an untaught particle flagging live becomes a chip on the error state like any word. The pieces:
- *Schema (v2):* `GrammarGlue` — unique `surface`, `kind` (`GlueKind`: particle/copula/interjection/adnominal/other — categorization only, validation treats all glue alike), nullable `importId`, `addedAt`. **Light provenance: presence in the table means approved** (a row only arrives via seed or review), and `importId == null` *is* the seed-origin marker — no status column. First `onUpgrade` migration in the app (schemaVersion 1→2, create + seed), with the project's first migration test (v1 file DB reopened as v2). Seeding is `insertOrIgnore` on the unique surface, idempotent (`glue_seed.dart`, pinned by test against the constant).
- *Validation:* `GenerationSeed` gains `glue` with const default `seedGrammarGlue` (the renamed `knownGrammarGlue` — kept as seed source and fixture default, no longer the live allowlist). `GlueMatcher` replaces the load-time `_glueFactoring` regex/`isKnownGlue` globals (same factoring semantics, built per `validateScope` call from `seed.glue`). `_eligibleCandidate` drops the `runes.length > 1` gate (single chars eligible; kanji and pure punctuation still excluded) — the particle-vs-word split moved to the UI.
- *Prompt:* the glue list joins `constraintContext` as a `GRAMMAR GLUE:` section, **sorted** so the cached block is byte-stable and only misses when taught material actually changes; the instruction prose now points at the list instead of hardcoding particles.
- *Backfill:* `ScopeBackfillService.commitGlue` — own small transaction (Imports provenance row with `{"source": "reading-backfill-glue"}` + linked `GrammarGlue` row), deliberately *not* through `runCommit` (word/structure-shaped merge logic); an already-present surface is a full no-op, no orphan Imports row. `addGlueToBunko` mirrors `addCandidateToBunko`; both share the extracted `_revalidateAfterBackfill`, so the self-heal (re-validate the rejected conversation, show it if it now passes) works identically for particles.
- *UI:* a single-char chip opens the new `GlueReviewSheet` (`lib/reading/glue_review_sheet.dart`) — same "Did your class teach this?" framing, surface headline, kind chips defaulting to Particle, no meaning required — **with a "This is a real word, not grammar" toggle** that swaps in the standard `VocabReviewCard`/word commit path for the rare genuine single-char word. Multi-char chips keep the existing word sheet unchanged.
*Also closed this session:* D3's residual quality check on the bumped generation model — the user generated several conversations on `claude-sonnet-5` and confirmed quality holds; the model constant stands.
*Verification:* `flutter analyze` clean; `flutter test` — 220/220 passing (23 new: 4 schema/seed/migration, 6 generator incl. widened-glue acceptance both directions, single-char candidate, punctuation-never-a-chip, prompt byte-stability, and the constant↔seed pin, 1 seed-repository glue load, 3 `commitGlue` db tests incl. the seeded/duplicate no-ops, 2 glue-sheet widget flows incl. the word-mode toggle, plus reworked existing candidate tests). Live simulator run: pending.

## 2026-07-19 — Session 16 (generation error instrumentation + truncation fix)

**D57. The reading screen's generation catch-all now classifies and logs the failure instead of blaming the connection for everything.** The `catch (_)` in `loadNext` (`reading_providers.dart`) discarded the exception and always showed "Couldn't reach the generator. Check your connection…", which was wrong for the majority of real failures. Now `catch (error, stack)` logs the raw error under `developer.log` name `reading.generate` (error + stack attached) and routes to `_generationFailureMessage`, which splits by cause: a `DioException` with no response → the connectivity message (unchanged, the only case it was ever right for); with a status → 401/403 "rejected the API key", 429 "busy (rate limit)", 5xx "temporarily overloaded", other "returned an error (N)"; anything else (parse/shape errors) → "replied, but the response could not be read". The old catch-all was pure guesswork; this makes the intermittent-failure question answerable from the console. *(Tests: 5 widget cases — connectionError, 429, 5xx, 401, FormatException — asserting the message split; existing failure tests assert the static "Couldn't generate that one" header and are unaffected.)*

**D58. Generation `max_tokens` raised 4000 → 8000, and a `max_tokens` stop now throws `GenerationTruncated` instead of a cryptic StateError — the D54 truncation fix applied to the generation path.** The `reading.generate` log (added D57) immediately caught a real case: `stop_reason=max_tokens` with **no complete text block at all**, which `parseGenerationResponse`'s `firstWhere` surfaced as `Bad state: no text block in response`. A fully token-annotated 6-line conversation (per-line text + per-token surface/vocab_id array) is larger output than it looks, and 4000 clipped it mid-JSON. Two-part fix mirroring D54: (1) `buildGenerationRequest`'s `maxTokens` default 4000 → 8000 — comfortable headroom (a ceiling, not a target: unused budget costs nothing) while staying in the safe non-streaming range for the blocking `dio` POST; (2) a new `GenerationTruncated` exception thrown on a `max_tokens` stop *before* the text-block lookup, so truncation never masquerades as "no text block". `loadNext` catches it above the generic bucket with its own message ("That conversation ran long and got cut off") and a dedicated `reading.generate` log line, so a recurrence against the raised ceiling stays visible. Deliberately not done: per-conversation chunking or a retry-at-higher-cap — 8000 should comfortably cover 6 lines; revisit only if the log shows truncation against the new ceiling. *(Tests: 1 parser test — a `max_tokens` stop with empty content throws `GenerationTruncated`, not `StateError`.)*
**D59. Conversation list: the generated-content cache's blind "Reread an earlier one" grew into a browsable list, and generation gained a display `topic` to title it (`features/conversation-list.md`).** The `features/generated-cache.md` "browsable history can grow out of phase 5" line, pulled forward because LRU-only reread felt random and gave the learner no control. The pieces: (1) **Titles at generation time** — the schema gained a required `topic` (short English noun-phrase, ≤~40 char), display metadata only (it does *not* participate in scope validation — the authority rule is untouched, titles are cosmetic), carried on `GeneratedConversation.topic` (defaulted `''` so pre-topic fixtures/payloads still construct) and stored in a new `GeneratedConversations.title` column. **No backwards compat** (spec §3): schema v3's migration *drops and recreates* the cache tables rather than backfill blank titles — cache is regenerable content, single-user, storage trivial; chosen explicitly over a vocab-derived title ("食べる, 水, …" is a word list, not a title). (2) **Store read/write surface** — `ConversationStore` gained `list()` (newest-first `ConversationSummary` rows, read without deserialising the payload), `byId()`, and `delete()` (DB row + link-row FK cascade); `AudioStore.deleteAudio()` removes the conventional `audio/conv_<id>` directory (a never-synthesised dir is a silent no-op). (3) **Screen** — `ConversationListScreen` + `conversationListProvider` (`lib/reading/conversation_list.dart`): swipe-to-delete with snackbar-undo done **optimistically** — `takeOut` drops the row from the visible list, the real DB/disk `commitDelete` fires only when the undo window lapses, so undo never has to resurrect a DB row or re-synthesise audio. Empty state and a load-error retry. Row metadata ("Jul 18 · practiced yesterday") is a pure, unit-tested formatter (no `intl` dependency). (4) **Opening a row** — a new `ReadingStart.conversation` entry (the family key is now a `ReadingRequest` record carrying the picked id); `readSpecific` renders the chosen cached conversation and stamps `lastPracticedAt`, but **Next generates fresh** (deliberately *not* a playlist — the list only seeds the first conversation, spec §2). The home button navigates here; the generation-failure error state keeps its one-tap LRU `leastRecentlyPracticed` reread (in that moment the learner wants something to read now, not a browsing detour), so that method keeps its one caller. Read-only otherwise — no rename/favorite/sort/search, and no cap/eviction — since SRS-driven ordering (phase 5) will likely reshape this screen. *(Tests: cache `list`/`byId`/`delete`/title round-trip; the meta-line/relative-date formatter; the list screen render/empty/swipe-undo + the notifier's optimistic-delete logic; the `ReadingStart.conversation` open-marks-practiced-Next-generates path; generator topic parse + round-trip.)*

*Verification:* `flutter analyze` clean; `flutter test` — 246/246 passing.

---

## 2026-07-19 — Session 17 (in-app image prep + extractor candidates/regions)

**D60. In-app image prep before send: the picked photo is baked upright, downscaled, and user-cropped on a preview screen, and the exact bytes sent are persisted as the import's `sourceImage`.** Closes the "in-app resize/auto-orient/crop" planned item. The live import previously sent the picked photo as-is (only `image_picker`'s `maxWidth`), so a sideways or cluttered photo went to the extractor untouched, and `sourceImage` was a picker temp path that could vanish. The pieces: (1) `lib/capture/image_prep.dart` — pure, UI-free, `compute()`-friendly top-level functions: `normalizeImage` (decode → `bakeOrientation` → downscale to 2000px long edge → JPEG), `cropAndEncode`, and `cropRectFromTransform` (inverts an InteractiveViewer matrix to the visible source rect). The `image` package (4.9) is the sole new dependency — hand-rolled crop over a crop widget/package, matching the dependency-averse furigana precedent. (2) `PhotoPrepScreen` (`lib/capture/screens/photo_prep_screen.dart`) — an always-shown frame-and-confirm step: the auto-oriented photo starts fit-to-frame under a fixed frame in an `InteractiveViewer` (`constrained: false` + pixel-sized child, so the controller matrix maps image pixels → viewport directly and the inversion is exact); what's visible on Send is what's sent. Viewport-crop, not drag-handles (user decision). (3) `PhotoImportScreen` wires pick → `compute(normalizeImage)` → prep → save under `<app documents>/worksheets/import_<ts>.jpg` → extract; the saved path (pixel-identical to what the extractor saw) becomes `sourceImage`, so it survives temp cleanup and D61's regions index into an image that still exists. Test seams (isolate `compute`, platform-channel path_provider, and real file IO all deadlock/unavailable under the widget-test fake clock — D51's lesson): injectable `imageNormalizer`/`cropper`/`documentsDirectory`, and the worksheet save uses synchronous IO (a one-shot on a user tap). *Deferred:* a cleanup policy for accumulated saved photos (small, not urgent). *(Tests: `image_prep_test.dart` — downscale, EXIF orientation-6 bake, quadrant crop + clamping, and the crop-math matrix cases; `photo_prep_screen_test.dart` — send-untouched ≈ whole image, injected-zoom sub-rect, back-pops-null; `photo_import_screen_test.dart` reworked to drive through the prep screen and assert the saved `sourceImage`.)*

**D61. Extractor kanji candidates + best-effort per-item crop regions.** Closes the two capture-loop.md §4 open questions (ranked kanji alternates; picture-derived drawing region). The kanji-chip design (§3) had always assumed ranked alternates, but the schema returned one guess, so the review card degraded to confirm-or-no-kanji. `extractionSchema`'s vocab item gained two required fields: `kanji_candidates` (array of strings, ranked best-first, ≤3, first entry must equal `kanji`, `[]` when no kanji printed) and `region` (`[left, top, right, bottom]` fractions 0–1 of the sent image, `[]` when unsure — no `maxItems`, which the constrained-decoding schema 400s on; the 4-element shape is enforced by `CropRegion.tryParse`). The prompt instructs both — alternates *only* when print is genuinely ambiguous (not padding with known kanji), and regions in the image's own upright axes (D60 already rotated it). `draft_from_extraction.dart` maps `kanji_candidates` into the chips the card already renders (deduped, confirmed-kanji forced first, singleton `[kanji]` fallback for legacy payloads and the fixture) and `CropRegion.tryParse(region)` into a new `VocabDraftItem.region` (clamps to [0,1], swaps inverted edges, null on wrong arity or <2%-side degenerate). `WorksheetPhotoBox` gained an optional `region` param: when set, a `_RegionImage` resolves the file to a `ui.Image` and a `CustomPainter` does one cover-fit `drawImageRect` cropping in *source-pixel* space (not layout space); null/degenerate/failed → whole-photo fallback, and the zoom preview always shows the whole photo (context is its point). Both review cards pass `item.region`; triage stays full-sheet. Vision boxes are approximate, so the region is framing only, never a data source — a bad box just falls back. Token cost is ~10–20 output tokens/item (dense sheet ≈ +1k), so the 16000 cap (D54) is unchanged. *Live re-validation against a real worksheet is a manual pending item.* *(Tests: `worksheet_extractor_test.dart` — schema requires the fields, prompt mentions them, parse round-trip; `draft_from_extraction_test.dart` — ranked/deduped/kanji-first mapping, legacy singleton fallback, `CropRegion.tryParse` clamp/swap/reject cases; `worksheet_photo_box_test.dart` — a region takes the CustomPaint crop path, not `Image.file`; `vocab_review_card_test.dart` — multiple candidates render as multiple chips + "No kanji".)*
